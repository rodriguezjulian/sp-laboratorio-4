import { OcultarPrimerasPipe } from './ocultar-primeras.pipe';

describe('OcultarPrimerasPipe', () => {
  it('create an instance', () => {
    const pipe = new OcultarPrimerasPipe();
    expect(pipe).toBeTruthy();
  });
});
