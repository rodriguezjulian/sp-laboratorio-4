import { ReemplazarNulosPipe } from './reemplazar-nulos.pipe';

describe('ReemplazarNulosPipe', () => {
  it('create an instance', () => {
    const pipe = new ReemplazarNulosPipe();
    expect(pipe).toBeTruthy();
  });
});
