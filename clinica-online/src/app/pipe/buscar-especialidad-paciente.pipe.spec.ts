import { BuscarEspecialidadPacientePipe } from './buscar-especialidad-paciente.pipe';

describe('BuscarEspecialidadPacientePipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarEspecialidadPacientePipe();
    expect(pipe).toBeTruthy();
  });
});
