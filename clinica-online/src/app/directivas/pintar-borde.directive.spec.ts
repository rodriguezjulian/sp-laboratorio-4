import { PintarBordeDirective } from './pintar-borde.directive';

describe('PintarBordeDirective', () => {
  it('should create an instance', () => {
    const directive = new PintarBordeDirective();
    expect(directive).toBeTruthy();
  });
});
