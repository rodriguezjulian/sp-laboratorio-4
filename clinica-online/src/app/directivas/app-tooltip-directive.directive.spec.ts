import { AppTooltipDirectiveDirective } from './app-tooltip-directive.directive';

describe('AppTooltipDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new AppTooltipDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
